# Kline_labeler

用于K线数据打标
以及可能的数据处理

使用
```
python Kline_labeler.py --symbol ETHUSDT --interval 1m --market futures --out_dir ./binance_data --fmt parquet [SAVE] 1 labels -> ./labels_ETHUSDT_1m.csv
```
